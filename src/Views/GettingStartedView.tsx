import PropTypes from "prop-types";
import React from "react";

interface Props {
  children?: any;
}

export const GettingStartedView = (props: Props) => {
  return <div>GettingStartedView</div>;
};
