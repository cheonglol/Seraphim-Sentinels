import React from 'react'

export const ExternallyReferredView = () => {
    return (
        // TODO: handle different referred source, add to URLsConstants.ts, this should be used in router.ts

        <div>ExternallyReferredView</div>
    )
}
